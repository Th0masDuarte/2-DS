//EXERCICIO 1 E 2
package javaapplication1;

import java.util.Scanner;
/*• Solicite ao usuário que digite um número.
• Após o usuário informar o número, executar um laço de repetição 
que exiba a tabuada deste número.
• Após a exibição da tabuada perguntar ao usuário se ele deseja 
continua (S/N), depois de ler a resposta do usuário, somete continuar 
o laço de repetição se ele respondeu <> “N”*/
public class JavaApplication1 {
    public static void main(String[] args) {
        String opcao="s";
    while(opcao.equals("s")){
    Scanner teclado=new Scanner(System.in);
        System.out.println("Digite um número para descobrir sua tabuada");
        int num1;
        num1=teclado.nextInt();
        System.out.println("Digite até qual número vai a tabuada");
        int num2;
        num2=teclado.nextInt();
        for(int tabuada=1;tabuada<=num2;tabuada++){
            int resultado=num1*tabuada;
            System.out.println(num1+" vezes "+tabuada+" é igual a: "+resultado);
        
    }
        System.out.println("Você deseja continuar (s para sim e n para não) ");
        opcao=teclado.next();}
       
}
}

//EXERCICIO 3
package javaapplication1;

import java.util.Scanner;

public class JavaApplication1 {
    public static void main(String[] args) {
        /*• Solicite ao usuário que digite um número.
• Após a leitura do numero perguntar ao usuário se ele deseja continua 
(S/N), depois de ler a resposta do usuário, somete continuar o laço de 
repetição se ele respondeu <> “N”.
• Caso ele responder “N” encerrar o laço de repetição exibindo:
 O maior número digitado
 O menor número digitado
 Quantos números fora lido*/
        Scanner teclado=new Scanner(System.in);
        int numero;
         int maior = Integer.MIN_VALUE;
        int menor = Integer.MAX_VALUE;
        int contador=0;
        String resposta;
        do{
            contador++;
            System.out.println("Digite um número: ");
            numero=teclado.nextInt();
            if(maior<numero){
                maior=numero;
            }
            if (numero<menor){
                menor=numero;
            }
            
            System.out.println("Deseja acabar com a repetição? (s ou n)");
            resposta=teclado.next();
        }while(resposta.equals("s")|| resposta.equals("S"));
        System.out.println("Maior número: "+maior);
        System.out.println("Menor número: "+menor);
        System.out.println("Quantidade de repetções: "+contador);
/* EXERCICIOS LISTA 2
      Dentro de uma rotina em JAVA criar um programa que:
• Solicite ao usuário que digite 5 números inteiros.
• Utilize um laço de repetição para realizar a leitura dos números.
• Após a leitura dos números, exibir:
✓ A soma de todos os números digitados.
✓ A média dos números digitados.
Dentro de uma rotina em loop criar um programa que:
• Solicite ao usuário que digite um número inteiro.
• Verifique se o número digitado é:
✓ Par
✓ Ímpar
• Após exibir o resultado perguntar ao usuário se deseja continuar (S/N).
• Continuar executando o programa enquanto a resposta for diferente de 'N'.
Dentro de uma rotina em loop criar um programa que:
• Solicite ao usuário que digite um número inteiro.
• Continuar solicitando números enquanto o usuário responder diferente de
'N'.
• Ao finalizar o programa exibir:
✓ Quantidade de números digitados.
✓ Soma dos números digitados.
✓ Média dos números digitados  
//EXERCICIO 1 LISTA 2
Scanner teclado=new Scanner(System.in);
int soma=0;
double media=0.00;
int num;
for(int i=0;i<=5;i++){
    System.out.println("Digite um número inteiro: ");
    num=teclado.nextInt();
    soma+=num;
}
media=soma/5;
        System.out.println("soma: "+soma);
        System.out.println("Média: "+media);*/
        
        //EXERCICIO 2
            Scanner teclado=new Scanner(System.in);
    String sn ="s";
    int num;
    while(!sn.equals("N")){
        System.out.println("Digite um numero inteiro: ");
        num=teclado.nextInt();
        if (num%2==0){
            System.out.println("È par");
        }else{
            System.out.println("è impar");
        }
        System.out.println("Deseja continuar? ");
        sn=teclado.next().toUpperCase();
        
    }

        //EXERCICIO 1 PRATICA DE JOptionPane
int num=1;
int soma=0;
String text;
while(num!=0){
    text=JOptionPane.showInputDialog("Digite um número: ");
    int inum = Integer.parseInt(text);
    if(inum==0){
        JOptionPane.showMessageDialog(null,"Programa encerrado. "+soma);
        break;
    }
     soma+=inum;
}
//EXERCICIO 2 VERIFICADOR DE SENHA
String senha="1234";
String tentativa="";
while(!senha.equals(tentativa)){
    tentativa=JOptionPane.showInputDialog("Digite sua senha: ");
    if(senha.equals(tentativa)){
        JOptionPane.showMessageDialog(null,"Senha correta");
    }else{
        JOptionPane.showMessageDialog(null, "senha incorreta, tente novamente");
        
    }
}
}
}



